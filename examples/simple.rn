
# Linear search algorithm
fun find(list, value) {
    for i = 0 to len(list) {
        if i == value {
            return i
        }
    }
    return -1
}

# var list = [1, 2, 3, 4, 5]
# var index = find(list, 3)
# print(index)

fun iseven(num) -> if (num%2) == 0 { print("even") } else { print("odd") }

class Algorithm {
    fun find(list, value) {
        for i = 0 to len(list) {
            if i == value {
                return i
            }
        }
        return -1
    }
}